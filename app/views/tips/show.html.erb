<div class = "wrapper">
  <%= render "shared/header" %>
  <div class="main-tips-show">
    <div class="tip-title">
      <p class="tip-title-show"><%= @tip.title %></p>
    </div>
    <div class="slider">
      <div class="image-show">
        <%if @tip.image.present?%>
        <%= image_tag @tip.image, class: 'image-show-content' %>
        <% else %>
        <%= image_tag "noimage.jpg", class:"image-show-content-none" %>
        <% end %>
      </div>
<% 
=begin
%>
      <div class="image-show">
        <%if @tip.image.present?%>
        <%= image_tag @tip.image, class: 'image-show-content' %>
        <% else %>
        <%= image_tag "noimage.jpg", class:"image-show-content-none" %>
        <% end %>
      </div>
      <div class="image-show">
        <%if @tip.image.present?%>
        <%= image_tag @tip.image, class: 'image-show-content' %>
        <% else %>
        <%= image_tag "noimage.jpg", class:"image-show-content-none" %>
        <% end %>
      </div>
<%
=end
%>
      </div>
    <div class="tip-category">
      <p>カテゴリー : <%= Category.data[@tip.category_id - 1][:name] %></p>
    </div>
    <div class="tip-contributer">
      <p>投稿者 : <%= link_to @tip.user.nickname, "/users/#{@tip.user.id}" %> </p>
    </div>
    <div class="tip-description">
      <p>説明 :  </p>
      <%= @tip.description %>
    </div>
  </div>
  <% if user_signed_in? && current_user.id == @tip.user.id %>
    <div class="edit-delete-area">
      <div class="show-edit">
        <div class="btn-edit">
          <%= link_to '編集', edit_tip_path(@tip.id)%>
        </div>
      </div>
      <div class="show-delete">
        <div class="btn-delete">
          <%= link_to '削除', tip_path(@tip.id), method: :delete %>
        </div>
      </div>
    </div>
  <% end %>
</div>